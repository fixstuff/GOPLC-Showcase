Part II is the development-environment tour. It covers the browser-based IDE (editor, debugger, HMI builder, analyzer), the YAML configuration format that ties a project together, the REST API surface that every other GoPLC tool is built on top of, the underlying runtime architecture, the built-in AI assistant and its autonomous control mode, Node-RED integration, the clustering boss-minion model, and the two MCP servers (local stdio and networked gateway) that let external AI agents control GoPLC fleets. Read this part to understand *how* GoPLC is used day-to-day.
